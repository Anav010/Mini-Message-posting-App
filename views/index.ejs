<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mini Whatsapp App</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h3>All Chats</h3>
    <form action="/chats/new" method="get">
        <button>New Chat+</button>
    </form>
    <% for(let chat of chats){ %>
        <div class="chat">
            <div class="to-from">
                <p>From: <%= chat.from %></p>
            </div>
            <div class="msgbox">
                <p>"<%= chat.msg %>"</p>
                <form method="get" action="/chats/<%= chat._id %>/edit">
                    <button style="background-color: violet">Edit</button>
                </form>
                <form action="/chats/<%= chat._id %>?_method=DELETE" method="post">
                    <button style="background-color: violet">Delete</button>
                </form>
            </div>
            <div class="to-from">
                <p>To: <%= chat.to %></p>
            </div>
            <hr>
            <%= chat.created_at.toString().split(" ").splice(0,5).join(" ") %>
        </div>
    <% } %>
</body>
</html>